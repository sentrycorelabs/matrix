name: Build and Push Docker Images

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/sentrycorelabs/matrix

permissions:
  contents: read
  packages: write

jobs:
  build-base:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_BASE }}
          tags: |
            type=raw,value=base
            type=semver,pattern={{version}},suffix=-base

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.base
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=base
          cache-to: type=gha,mode=max,scope=base

  build-runtime-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_BASE }}
          tags: |
            type=raw,value=runtime-node
            type=semver,pattern={{version}},suffix=-runtime-node

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.runtime-node
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=runtime-node
          cache-to: type=gha,mode=max,scope=runtime-node

  build-runtime-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_BASE }}
          tags: |
            type=raw,value=runtime-python
            type=semver,pattern={{version}},suffix=-runtime-python

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.runtime-python
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=runtime-python
          cache-to: type=gha,mode=max,scope=runtime-python

  build-runtime-php:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_BASE }}
          tags: |
            type=raw,value=runtime-php
            type=semver,pattern={{version}},suffix=-runtime-php

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.runtime-php
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=runtime-php
          cache-to: type=gha,mode=max,scope=runtime-php
